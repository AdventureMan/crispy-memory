{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "node server serving a static page and acting as an API proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "matt keenan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  }
}
